---
layout: layouts/base.njk
title: "A Dog's Guide — Dog-Friendly Travel, Honestly Rated"
description: "Dog-friendly travel, planned properly. Independently researched destination guides and bespoke trip planning — every venue rated on the Golden Paw Scale."
footerType: home
permalink: /
---

<!-- ─── Hero ─── -->
<section class="hero">
  <div class="container">
    <div class="hero-content">
      <p class="hero-eyebrow">Dog-friendly travel, honestly rated</p>
      <h1>Find places that <em>actually</em> welcome your dog</h1>
      <p class="hero-desc">Independently researched guides to the world's best dog-friendly destinations — every venue rated on the Golden Paw Scale, so you know exactly what you're getting before you go.</p>
      <div class="hero-actions">
        <a href="#destinations" class="btn btn-primary">Explore destinations</a>
        <a href="#scale" class="btn btn-outline">How we rate places</a>
      </div>
    </div>
  </div>
</section>

<!-- ─── Destinations ─── -->
<section id="destinations">
  <div class="container">
    <div class="section-header">
      <p class="section-eyebrow">Our guides</p>
      <h2>Destinations</h2>
      <p>Every guide is independently researched. No sponsored content, no paid placements — just honest venue ratings for dog owners who refuse to compromise on where they go.</p>
    </div>
    <div class="dest-carousel" aria-label="Destinations">
      <div class="dest-carousel-track">
        <div class="dest-carousel-slide">
          <a href="/london/" class="dest-card">
            <img src="/images/london-hero.webp" alt="London" class="dest-card-cover" loading="lazy" />
            <span class="dest-card-link">View full guide</span>
          </a>
        </div>
        <div class="dest-carousel-slide">
          <a href="/lake-district/" class="dest-card">
            <img src="/images/lake-district-hero.webp" alt="Lake District" class="dest-card-cover" loading="lazy" />
            <span class="dest-card-link">View full guide</span>
          </a>
        </div>
      </div>
      <div class="scale-carousel-controls">
        <button class="carousel-btn dest-carousel-btn--prev" aria-label="Previous destination">
          <i class="fa-solid fa-chevron-left"></i>
        </button>
        <div class="carousel-dots" id="dest-dots">
          <button class="carousel-dot carousel-dot--active" aria-label="London"></button>
          <button class="carousel-dot" aria-label="Lake District"></button>
        </div>
        <button class="carousel-btn dest-carousel-btn--next" aria-label="Next destination">
          <i class="fa-solid fa-chevron-right"></i>
        </button>
      </div>
    </div>
  </div>
</section>

<!-- ─── About ─── -->
<section id="about">
  <div class="container">
    <div class="about-grid">
      <div class="about-text">
        <p class="section-eyebrow">Who we are</p>
        <h2>A different kind of travel guide</h2>
        <p>A Dog's Guide is built for people who travel with their dogs and refuse to compromise. We research every destination thoroughly — vetting every venue, reading every review, calling ahead when needed — so you can travel anywhere in the world with confidence.</p>
        <p>Dog-friendly doesn't always mean dog-welcome. We built the Golden Paw Scale to tell the difference: a rating that reflects what a venue actually offers your dog, not just whether dogs are technically allowed.</p>
        <p>No sponsored content. No paid placements. If it's in a guide, it earned its place.</p>
      </div>
    </div>
  </div>
</section>

<!-- ─── Golden Paw Scale ─── -->
<section id="scale">
  <div class="container">
    <div class="section-header" style="max-width: 620px;">
      <p class="section-eyebrow">How we rate</p>
      <h2>The Golden Paw Scale</h2>
      <p>Not all dog-friendly venues are created equal. A water bowl by the door is not the same as a welcome treat, a dog menu, and a member of staff who actually gets down to say hello.</p>
    </div>
    <div class="scale-carousel" aria-label="Golden Paw Scale slides">
      <div class="scale-carousel-track">
        <div class="scale-carousel-slide">
          <img src="/images/rate-slide-gold.webp" alt="Golden Paw — our highest rating" loading="lazy" />
        </div>
        <div class="scale-carousel-slide">
          <img src="/images/rate-slide-silver.webp" alt="Silver — dogs genuinely welcomed" loading="lazy" />
        </div>
        <div class="scale-carousel-slide">
          <img src="/images/rate-slide-bronze.webp" alt="Bronze — dogs are welcomed" loading="lazy" />
        </div>
      </div>
      <div class="scale-carousel-controls">
        <button class="carousel-btn carousel-btn--prev" aria-label="Previous slide">
          <i class="fa-solid fa-chevron-left"></i>
        </button>
        <div class="carousel-dots">
          <button class="carousel-dot carousel-dot--active" aria-label="Slide 1"></button>
          <button class="carousel-dot" aria-label="Slide 2"></button>
          <button class="carousel-dot" aria-label="Slide 3"></button>
        </div>
        <button class="carousel-btn carousel-btn--next" aria-label="Next slide">
          <i class="fa-solid fa-chevron-right"></i>
        </button>
      </div>
    </div>
    <p class="scale-footer-tagline">Independently assessed · Never sponsored · Always honest</p>
  </div>
</section>

<!-- ─── Instagram ─── -->
<div class="instagram-strip">
  <div class="container">
    <p>Follow the guides as they're built</p>
    <a href="https://www.instagram.com/adogsguide" target="_blank" rel="noopener">@adogsguide</a>
  </div>
</div>

<script>
  function initCarousel(trackSel, slideSel, dotsSel, prevSel, nextSel) {
    const track = document.querySelector(trackSel);
    const slides = document.querySelectorAll(slideSel);
    const dots = document.querySelectorAll(dotsSel);
    let current = 0;
    function goTo(index) {
      current = (index + slides.length) % slides.length;
      track.style.transform = `translateX(-${current * 100}%)`;
      dots.forEach((d, i) => d.classList.toggle('carousel-dot--active', i === current));
    }
    document.querySelector(prevSel).addEventListener('click', () => goTo(current - 1));
    document.querySelector(nextSel).addEventListener('click', () => goTo(current + 1));
    dots.forEach((dot, i) => dot.addEventListener('click', () => goTo(i)));
    let startX = 0;
    track.addEventListener('touchstart', e => startX = e.touches[0].clientX, { passive: true });
    track.addEventListener('touchend', e => {
      const diff = startX - e.changedTouches[0].clientX;
      if (Math.abs(diff) > 40) goTo(current + (diff > 0 ? 1 : -1));
    });
  }
  initCarousel('.scale-carousel-track', '.scale-carousel-slide', '.scale-carousel .carousel-dot', '.carousel-btn--prev', '.carousel-btn--next');
  initCarousel('.dest-carousel-track', '.dest-carousel-slide', '#dest-dots .carousel-dot', '.dest-carousel-btn--prev', '.dest-carousel-btn--next');
</script>
